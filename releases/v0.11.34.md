# Release v0.11.34

## Release Date
2025-07-14

## Stage 4: Process Mode Components Removal

### Overview
This release completes Stage 4 of the refactoring process - complete removal of Process Mode functionality, leaving only OneShoot mode as the primary operational mode.

### Major Changes

#### =Ñ Removed Components
1. **ProcessSessionManager.ts** - Complete removal of Process mode implementation (493 lines)
2. **ProcessSessionFactory** - Factory class for Process sessions
3. **Process mode conditions** - All `mode === 'process'` checks removed
4. **Process-specific methods**:
   - `startClaudeCodeInProcess()`
   - `setupProcessEventHandlers()`
   - `handleProcessData()`
   - `handleProcessExit()`
   - `handleProcessError()`
   - `handleProcessInteractivePrompt()`

#### =à Code Cleanup
1. **Session interface** - Removed `processSession?: ProcessSessionManager` field
2. **DualSessionManager** - Removed all Process mode handling logic
3. **Type definitions** - Updated `Omit<Session>` types to exclude only 'terminal'
4. **Terminal mode cleanup** - Removed remaining Terminal mode conditions
5. **Unused code removal**:
   - `handleResponseFromTerminal()`
   - `handleServiceInfoFromTerminal()`
   - `startClaudeCode()`
   - `sessionMonitoringStatus` field

### Technical Impact
- **Lines removed**: ~800+
- **TypeScript errors fixed**: All compilation errors resolved
- **Build status**: Clean build with no TypeScript errors

### Migration Notes
- Process mode is no longer available
- Terminal mode references have been removed
- Only OneShoot mode remains functional

### Testing Status
- Build passes without errors
- TypeScript compilation successful
- Webpack bundle generated successfully

### Next Steps (Stage 5)
- Rename DualSessionManager ’ OneShootSessionManager
- Remove remaining mode checks and session type field
- Simplify session creation logic