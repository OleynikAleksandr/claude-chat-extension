# Release v0.11.36 - UI and Types Cleanup (Stage 6)

## <¯ Overview
**Final Cleanup:** Removed all Terminal/Process references from comments, UI, and dependencies.

## ( Key Changes

### 1. Removed Legacy Files
-  Deleted `/src/bidirectional-bridge/` directory (5 files)
-  Deleted `/src/terminalManager.ts`
-  Total: 6 legacy files removed

### 2. Cleaned Comments and Imports
-  Removed commented Terminal/Process imports from `extension.ts`
-  Removed debugTerminals command
-  Updated comments to reflect OneShoot-only architecture

### 3. Updated UI Components
-  `onNewProcessSession` ’ `onNewSession` in TabBar
-  Updated CSS comments from "Terminal" to "Raw Monitor"
-  Type references cleaned (removed Omit<Session, 'terminal'>)

### 4. Cleaned Types
-  Removed `TerminalStatus` interface from `types.ts`
-  Removed `TERMINAL_BUSY` error code
-  Updated `BidirectionalSessionInfo.mode` to only 'oneshoot'
-  Changed `terminalActive` ’ `sessionActive`

### 5. Dependency Cleanup
-  Removed `node-pty` dependency (terminal emulation library)
-  Kept only essential dependencies

## =Ê Statistics
- **Files deleted:** 7
- **Files modified:** 8
- **Dependencies removed:** 1
- **Build status:**  Clean (no errors)

## =' Technical Details

### Deleted Files:
1. `/src/bidirectional-bridge/BidirectionalBridge.ts`
2. `/src/bidirectional-bridge/JsonlReader.ts`
3. `/src/bidirectional-bridge/ResponseParser.ts`
4. `/src/bidirectional-bridge/TerminalResponseReader.ts`
5. `/src/bidirectional-bridge/index.ts`
6. `/src/terminalManager.ts`

### Modified Files:
1. `src/extension.ts`
2. `src/types.ts`
3. `src/multi-session/webview/components/TabBar.tsx`
4. `src/multi-session/webview/components/TabBar.css`
5. `src/multi-session/webview/components/App.tsx`
6. `src/multi-session/types/Session.ts`
7. `package.json`

### Next Steps:
- Stage 7: Final cleanup and v0.12.0 major release

## =€ Installation
```bash
code --install-extension claude-chat-0.11.36.vsix
```

---
*Part of the OneShoot-only refactoring initiative*